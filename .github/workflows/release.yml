#name: Release
#
#on:
#  push:
#    tags:
#      - 'v*'
#
#jobs:
#  create-release:
#    name: Create Release
#    runs-on: ubuntu-latest
#    outputs:
#      upload_url: ${{ steps.create_release.outputs.upload_url }}
#
#    steps:
#      - name: Create Release
#        id: create_release
#        uses: actions/create-release@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          tag_name: ${{ github.ref }}
#          release_name: Release ${{ github.ref }}
#          draft: false
#          prerelease: false
#
#  build-release:
#    name: Build Release - ${{ matrix.config.name }}
#    needs: create-release
#    runs-on: ${{ matrix.config.os }}
#    strategy:
#      matrix:
#        config:
#          - {
#              name: "Windows",
#              os: windows-latest,
#              generator: "Visual Studio 17 2022",
#              artifact: "windows"
#            }
#          - {
#              name: "Linux",
#              os: ubuntu-latest,
#              generator: "Unix Makefiles",
#              artifact: "linux"
#            }
#          - {
#              name: "macOS",
#              os: macos-latest,
#              generator: "Unix Makefiles",
#              artifact: "macos"
#            }
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Configure CMake
#        run: cmake -B build -G "${{ matrix.config.generator }}" -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF -DBUILD_EXAMPLES=ON
#
#      - name: Build
#        run: cmake --build build --config Release
#
#      - name: Package
#        run: |
#          cd build
#          cpack -C Release
#
#      - name: Upload Release Asset
#        uses: actions/upload-release-asset@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          upload_url: ${{ needs.create-release.outputs.upload_url }}
#          asset_path: ./build/CwAPI3DEx-*.tar.gz
#          asset_name: CwAPI3DEx-${{ matrix.config.artifact }}.tar.gz
#          asset_content_type: application/gzip
